# Copy this file to .env and fill in real values before running docker compose up.
# Generate KEY / SECRET with: openssl rand -hex 16   /   openssl rand -hex 32

KEY=
SECRET=

ADMIN_EMAIL=admin@osmon.com
ADMIN_PASSWORD=

DB_DATABASE=directus
DB_USER=directus
DB_PASSWORD=
